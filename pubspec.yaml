name: h3_core
version: 1.0.6
description: >-
  Dart/Flutter bindings for Uber's H3 hexagonal geospatial indexing library.
  Uses dart:ffi on native platforms and h3-js on web — same API everywhere.
repository: https://github.com/wayqteam/h3_core
homepage: https://github.com/wayqteam/h3_core
issue_tracker: https://github.com/wayqteam/h3_core/issues
documentation: https://pub.dev/documentation/h3_core/latest/
topics: [h3, geospatial, ffi, mapping, hexagonal]

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  ffi: ^2.1.5
  hooks: ^2.0.0
  code_assets: ^1.1.0
  native_toolchain_c: ^0.19.0
  glob: ^2.1.1
  logging: ^1.3.0

dev_dependencies:
  ffigen: ^20.1.1
  test: ^1.25.0
  lints: ^6.1.0
